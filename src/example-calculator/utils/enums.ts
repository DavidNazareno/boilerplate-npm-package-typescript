/**
 * OperationType Enum
 * This enum defines the types of operations supported by the calculator.
 */
export enum OperationType {
  ADD = 'ADD',
  SUBTRACT = 'SUBTRACT',
  MULTIPLY = 'MULTIPLY',
  DIVIDE = 'DIVIDE',
}
